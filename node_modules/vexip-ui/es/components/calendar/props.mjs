import "../../common/config/src/index.mjs";
import { buildProps, eventProp, booleanProp } from "../../common/config/src/props.mjs";
const calendarProps = buildProps({
  value: [Number, String, Date],
  year: Number,
  month: Number,
  weekDays: Array,
  weekStart: Number,
  today: [Number, String, Date],
  disabledDate: Function,
  onSelect: eventProp()
});
const calendarPanelProps = buildProps({
  value: [Number, String, Date, Array],
  year: Number,
  month: Number,
  weekDays: Array,
  weekStart: Number,
  today: [Number, String, Date],
  disabledDate: Function,
  isRange: booleanProp,
  valueType: String,
  min: [Number, String, Date],
  max: [Number, String, Date],
  onSelect: eventProp(),
  onHover: eventProp()
});
export { calendarPanelProps, calendarProps };
