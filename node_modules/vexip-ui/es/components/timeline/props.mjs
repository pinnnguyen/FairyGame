import "../../common/config/src/index.mjs";
import { buildProps, booleanProp, eventProp } from "../../common/config/src/props.mjs";
const timelineProps = buildProps({
  pending: booleanProp,
  bothSides: booleanProp,
  dashed: booleanProp,
  lineColor: String,
  spacing: [Number, String],
  flip: booleanProp,
  horizontal: booleanProp,
  onSignalClick: eventProp()
});
const timelineItemProps = buildProps({
  type: String,
  color: String,
  label: [Number, String],
  dashed: booleanProp,
  lineColor: String,
  spacing: [Number, String],
  onSignalClick: eventProp()
});
export { timelineItemProps, timelineProps };
