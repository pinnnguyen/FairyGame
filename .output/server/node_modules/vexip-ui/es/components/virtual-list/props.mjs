import "../../common/config/src/index.mjs";
import { buildProps, eventProp } from "../../common/config/src/props.mjs";
const virtualListProps = buildProps({
  items: Array,
  itemSize: Number,
  itemFixed: Boolean,
  idKey: String,
  defaultKeyAt: [Number, String, Symbol],
  bufferSize: Number,
  listTag: String,
  itemsTag: String,
  itemsAttrs: Object,
  onScroll: eventProp(),
  onResize: eventProp()
});
export { virtualListProps };
