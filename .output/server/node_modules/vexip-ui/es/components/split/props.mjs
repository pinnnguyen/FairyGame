import "../../common/config/src/index.mjs";
import { buildProps, booleanProp, eventProp } from "../../common/config/src/props.mjs";
const splitProps = buildProps({
  value: Number,
  min: Number,
  max: Number,
  vertical: booleanProp,
  noTransition: booleanProp,
  lazy: booleanProp,
  canFull: booleanProp,
  onChange: eventProp(),
  onFull: eventProp(),
  onReset: eventProp(),
  onMoveStart: eventProp(),
  onMove: eventProp(),
  onMoveEnd: eventProp()
});
export { splitProps };
