import "../../common/config/src/index.mjs";
import { buildProps, sizeProp, booleanProp, eventProp } from "../../common/config/src/props.mjs";
const paginationProps = buildProps({
  size: sizeProp,
  total: Number,
  noBorder: booleanProp,
  background: booleanProp,
  pageSize: Number,
  sizeOptions: Array,
  maxCount: Number,
  active: Number,
  disabled: booleanProp,
  disableItem: Function,
  turnPageCount: Number,
  pageJump: booleanProp,
  pageCount: booleanProp,
  pageTotal: booleanProp,
  itemUnit: String,
  plugins: Array,
  noTitle: booleanProp,
  onChange: eventProp(),
  onPageSizeChange: eventProp()
});
export { paginationProps };
