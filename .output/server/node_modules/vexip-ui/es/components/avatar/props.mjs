import "../../common/config/src/index.mjs";
import { buildProps, booleanProp, eventProp } from "../../common/config/src/props.mjs";
const avatarProps = buildProps({
  size: [Number, String],
  src: String,
  icon: Object,
  circle: booleanProp,
  alt: String,
  fit: String,
  srcSet: String,
  gap: Number,
  iconScale: Number,
  fallbackSrc: String,
  color: String,
  background: String,
  onError: eventProp(),
  onClick: eventProp()
});
const avatarGroupProps = buildProps({
  size: [Number, String],
  options: Object,
  circle: booleanProp,
  max: Number,
  showTip: booleanProp,
  tipTrigger: String,
  vertical: booleanProp,
  offset: Number,
  restColor: String,
  restBackground: String
});
export { avatarGroupProps, avatarProps };
